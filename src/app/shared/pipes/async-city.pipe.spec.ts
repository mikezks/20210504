import { AsyncCityPipe } from './async-city.pipe';

describe('AsyncCityPipe', () => {
  it('create an instance', () => {
    const pipe = new AsyncCityPipe();
    expect(pipe).toBeTruthy();
  });
});
